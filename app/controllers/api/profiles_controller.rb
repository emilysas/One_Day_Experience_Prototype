# class API::ProfilesController < ApplicationController
  
  # served at http://localhost:3000//api/profiles.json?page=1,2,3,etc..
  # def index
    # @result = Profile.paginate(:page => params[:page], :per_page => 3).select([:id, :name, :company, :info, :profession_id, :image_file_name])

    # respond_to do |format|
#       format.json { render :json => @result.as_json( :methods => [:image_url, :profile_job])  }
#     end
#   end
# end
